#ifndef FS_SYMLINK_HPP
#define FS_SYMLINK_HPP

#include <filesystem>
#include <string_view>
#include <cstdlib>

inline namespace ool {
    namespace fs {

        // A wrapper around a symlink
        class symlink final {
        private:
            // TODO: Finish symlink

        };
    }
}

#endif