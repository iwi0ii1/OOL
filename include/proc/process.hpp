#ifndef PROC_PROCESS_HPP
#define PROC_PROCESS_HPP
#endif

#ifdef _WIN32

#elif __unix__

#endif


namespace alt {
    namespace proc {

        // A wrapper around a process
        class process final {
            // TODO: Fill it up
        };
    }
}