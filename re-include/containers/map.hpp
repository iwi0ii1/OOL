#pragma once

#include "../base/iterators.hpp"

namespace asl::containers {
    // Sorted map
    template<typename T>
    class map final : public base::iterator_common<T*, map> {
    public:
        // TODO: Sorted map
    };
}