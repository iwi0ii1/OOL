#pragma once

#include "../base/iterators.hpp"

namespace asl::containers {
    // Hashset
    template<typename T>
    class hashset final : public base::iterator_common<T*, hashset> {
    public:
        // TODO: Hashset
    };
}