#pragma once

#include "../base/iterators.hpp"

namespace asl::containers {
    // Vector / Dynamic array
    template<typename T>
    class vector final : public base::iterator_common<T*, vector> {
    public:
        // TODO: Vector
    };
}